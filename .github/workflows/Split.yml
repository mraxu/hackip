name: Split Large File and Push

on:
  push:
    branches:
      - main 

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2
      with:
        ref: main  # 确保使用正确的分支名

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip

    - name: Split the file
      env:
        FILE_PATH: ip-list.txt
        OUTPUT_PREFIX: split-output  # 替换为输出文件的前缀
      run: |
        split_file.py ${{ env.FILE_PATH }} ${{ env.OUTPUT_PREFIX }}

    - name: Add and Commit split files
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add split-output_*.txt
        git commit -m "Add split files"

    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: main  # 推送到哪个分支，如果需要推送到新分支，可以改为新的分支名
